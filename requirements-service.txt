# for the server part
fire~=0.4.0
flask~=2.0.1
flask-cors~=3.0.10

# for the analysis
numpy
pandas==1.2.4
sentence-transformers==2.1.0
tensorflow-cpu~=2.5.0
tensorflow-hub~=0.12.0

# disabled because they don't matter for the service (used in the cmdline frontend)
#matplotlib~=3.4.2       # heatmap and graphs (palette)
#networkx~=2.5.1         # network graphs (1/2: easy build)
#python-igraph~=0.9.4    # network graphs (2/2: best layout/plot)
#pycairo~=1.20.1         # network graphs: render PNGs
#seaborn==0.11.1         # heatmap
#tensorflow-text         # disabled because pulls in tensorflow (not tf-cpu) again
